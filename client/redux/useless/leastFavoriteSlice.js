// reducer for favorites component

import { createSlice } from '@reduxjs/toolkit';

export const leastFavoriteSlice = createSlice({
    name: 'leastFavorite',
    initialState,
    reducers: {}
});

export default leastFavoriteSlice.reducer;